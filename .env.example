# ===========================================
# Beef With Bezos - Environment Configuration
# ===========================================

# Vercel KV (Redis) - Required for production
# Create a KV database at: https://vercel.com/dashboard/stores
# Leave empty for local development (uses in-memory storage)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Admin Authentication
# REQUIRED in production - the app will fail to start without this
# Use a strong, unique password (minimum 12 characters recommended)
# Example: openssl rand -base64 32
ADMIN_PASSWORD=

# Note: In local development, if ADMIN_PASSWORD is not set,
# it defaults to "bezos123" for convenience.
# This default is NEVER used in production.
